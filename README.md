
  # delivery app

  This is a code bundle for delivery app. The original project is available at https://www.figma.com/design/ot58dOmvSXebBCD7yqes2X/delivery-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  